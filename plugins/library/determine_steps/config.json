{
  "schema": 1,
  "description": "Determine what to do next in a conversation and pass back an action JSON object",
  "execution_settings": {
    "default": {
      "max_tokens": 4000,
      "temperature": 0.3,
      "top_p": 0.0,
      "presence_penalty": 0.0,
      "frequency_penalty": 0.0
    }
  },
  "input_variables": [
    {
      "name": "history",
      "description": "The conversation history so far.",
      "default": ""
    }
  ],
  "output_variables": [
    {
      "name": "output",
      "description": "The action to take next in the conversation.",
      "default": {"action": "search"}
    }
  ]
}