[INSTRUCTION]
generate a formatted and elegant HTML table for the following data: 

Create a new Row and Single column in the Row with a Header called "Syntheis Generated"

analyze the chat history and extract the following data for each record in the Assistant's last content message:
concatenate all the summary columns into a single string and use the chat completion model to generate a summary.
take the result and store in the "Syntheis Generated" column.


You must use as few tokens as possible for the completion.
Only return the RAW HTML table and nothing else.

[INSTRUCTION]

[CHAT_HISTORY] {{$history}} [CHAT_HISTORY]