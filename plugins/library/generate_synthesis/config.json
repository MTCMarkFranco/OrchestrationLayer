{
  "schema": 1,
  "description": "A Semantic Function that generates a synthesis (Summarization) of the search results obtained from the search records.",
  "execution_settings": {
    "default": {
      "max_tokens": 4000,
      "temperature": 0.7,
      "top_p": 0.0,
      "presence_penalty": 0.0,
      "frequency_penalty": 0.0
    }
  },
  "input_variables": [
    {
      "name": "input",
      "description": "The records to generate an Abstractive summary from a.k.a syhtnesis",
      "default": ""
    }
  ]
}