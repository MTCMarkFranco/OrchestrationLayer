{
  "schema": 1,
  "description": "A Semantic Function that generates a synthesis of the search results obtained from the input, if it exists.",
  "execution_settings": {
    "default": {
      "max_tokens": 4000,
      "temperature": 0.0,
      "top_p": 0.0,
      "presence_penalty": 0.0,
      "frequency_penalty": 0.0
    }
  },
  "input_variables": [
    {
      "name": "input",
      "description": "The last records returned from the Search query.",
      "default": ""
    }
  ],
  "output_variables": [
    {
      "name": "output",
      "description": "The synthesis of the search results obtained from the input, if it exists."
    }
  ]
}