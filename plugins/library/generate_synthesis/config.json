{
  "schema": 1,
  "description": "A Semantic Function that generates a synthesis of the search results obtained from the history of the conversation, if it exists.",
  "execution_settings": {
    "default": {
      "max_tokens": 4000,
      "temperature": 0.7,
      "top_p": 0.0,
      "presence_penalty": 0.0,
      "frequency_penalty": 0.0
    }
  },
  "input_variables": [
    {
      "name": "history",
      "description": "The conversation history so far.",
      "default": ""
    },
    {
      "name": "userinput",
      "description": "The current query or request made by the user.",
      "default": ""
    }
  ]
}